name: tomeui_clickwheel
description: >-
  Click-wheel input and focus navigation for TomeUI, with media controls,
  configurable press and hold gestures, and wheel-driven lists and grids.
version: 0.3.4
repository: https://github.com/artificery-dev/tomeui/tree/main/clickwheel
issue_tracker: https://github.com/artificery-dev/tomeui/issues
topics:
  - widgets
  - input
  - navigation
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  tomeui: ^0.3.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
